const baseUrl = 'https://swapi.co/api/';
const endpoint = {
  People: `${baseUrl}people/`,
  Planets: `${baseUrl}planets/`,
  Vehicles: `${baseUrl}vehicles/`
};

const count = 9;

export { endpoint, count };
